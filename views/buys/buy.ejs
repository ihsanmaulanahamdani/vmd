<%- include ../partials/_header.ejs %>
  <body>
    <div>
      <h1 class="center">Buy Page</h1>

<table class="table">
  <tr>
    <th>NAME</th>
    <th>STOCK</th>
    <th>PRICE</th>
    <th>ACTION</th>
    <th></th>
  </tr>
  <% Buys.forEach(function(buy){%>
    <tr>
      <!-- <td><%= //buy.id%></td> -->
      <td><%= buy.name%></td>
      <td><%= buy.stock%></td>
      <td><%= buy.price%></td>
    <td><a href="/buys/<%= buy.id %>" class="btn btn-success">Buy</a></td>
    <!-- <td><a href="/items/delete/<%= //item.id %>" class="btn btn-success" onclick="return confirm('Are you sure you want to delete this Item?')">Delete</a></td> -->
  </tr>
  <%})%>

</table>
</div>

</div>
</div>
</body>
</html>
